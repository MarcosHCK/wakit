/*
 * Autogenerated from DBus interface definitions, do not modify this file,
 * instead modify your interface source code (xml or vala)
 */
{% if has_signals %}
import { SignalSpec, IWithSignals } from '@wakit-companion/types/signal'
{% endif %}

declare global
{

  namespace bridge
    {

      {% for (dbus_info, source) in infos %}
      /**
       * name: {{ dbus_info.name }}
       * source: {{ source }}
       */
      namespace {{ dbus_info.name.split ('.') [-1] }}
        {

        {% for info_ in (dbus_info.methods or []) %}
          function {{ info_.name }} (): {{ info_.out_args | out_signature }};
        {% endfor %}
        }
      {% endfor %}
    }
}

export { }